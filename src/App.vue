<template>
    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">
                <h1>Filters & Mixins</h1>
                <!-- Exercise 1) -->
                <!-- Build a local Filter which reverses the Text it is applied on -->
                <input type="text" v-model="beerFilter">
                <ul>
                    <li v-for="beer in beers" v-bind:key="beer">{{ beer | reverses }} {{ beer | countLetter }}</li>
                </ul>
                <hr>
                <!-- Exercise 2 -->
                <!-- Build a global Filter which counts the length of a word and it appends it -->
                <!-- Like this: "Test" => Gets Filtered to => "Test (4)" -->

                <!-- Exercise 3 -->
                <!-- Do the same as in Exercises 1 & 2, now with Computed Properties -->
                <ul>
                    <li v-for="beer in filterBeer" v-bind:key="beer">{{ beer }}</li>
                </ul>
                <!-- Exercise 4 -->
                <!-- Share the Computed Property rebuilding Exercise 2 via a Mixin -->
            </div>
        </div>
    </div>
</template>

<script>
    import { beerMixin } from './beerMixin'

    export default {
        mixins: [beerMixin],
        filters: {
            reverses: function(element) {
                return element.split('').reverse().join('')
            }
        }
    }
</script>

<style>
</style>
